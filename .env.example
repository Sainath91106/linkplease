# Copy this file to .env for local development
# Do NOT commit your real .env file.

# Required for webhook signature verification and API calls
PSEUDOGRAM_API_KEY=your_key_here

# Optional (default shown)
PSEUDOGRAM_BASE_URL=https://pseudogram-api.onrender.com

# Optional SQLite database path (default: linkplease.db at project root).
# For Render with a persistent disk mounted at /var/data, use:
# DB_PATH=/var/data/linkplease.db
DB_PATH=linkplease.db
